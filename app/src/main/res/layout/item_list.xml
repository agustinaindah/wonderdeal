<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/layItemList"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="?android:attr/selectableItemBackground"
    android:clickable="true"
    android:padding="5dp">

    <ImageView
        android:id="@+id/imgItemList"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:padding="5dp"
        android:src="@drawable/ic_accounting48"/>
    <!--android:background="@drawable/ic_accounting48"-->

    <TextView
        android:id="@+id/txtItemList"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignTop="@+id/imgItemList"
        android:layout_toEndOf="@+id/imgItemList"
        android:layout_toRightOf="@+id/imgItemList"
        android:padding="10dp"
        android:text="Payment Confirm"
        android:textSize="16sp" />

    <FrameLayout
        android:id="@+id/frItemList"
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:layout_alignBottom="@+id/imgItemList"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:background="@color/grey_300" />

</RelativeLayout>